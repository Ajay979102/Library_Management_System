# Library_Management_System
Library Management System developed in C using file handling concepts. The project can add, search, issue, return, and delete book records. Book data is stored in a CSV file for simple data management. This project demonstrates CRUD operations and string handling in C. Created for practicing file handling and basic database-like operations.
